{
    "buildCommand": "bash build.sh",
    "builds": [{
      "src": "setup/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.14.0" }
    }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "setup/wsgi.py"
      }
    ]
}